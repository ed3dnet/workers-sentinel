# Local development tool activation only. Local Workers development needs no
# secrets: `wrangler dev` runs under Workerd without Cloudflare authentication,
# and the local supervisor strips CLOUDFLARE_*/CF_* credentials from children.
# Remote deployment (when added) uses wrangler's own interactive OAuth login.
use mise
