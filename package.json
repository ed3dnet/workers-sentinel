{
	"name": "workers-sentinel",
	"version": "1.0.0",
	"private": true,
	"description": "A self-hosted, Sentry-compatible error tracking system running on Cloudflare Workers",
	"license": "MIT",
	"type": "module",
	"scripts": {
		"dev": "pnpm --filter workers-sentinel dev",
		"build": "pnpm --filter @workers-sentinel/dashboard build && pnpm --filter workers-sentinel build",
		"deploy": "pnpm build && pnpm --filter workers-sentinel deploy",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"format": "biome format --write .",
		"typecheck": "pnpm -r typecheck"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.11",
		"@changesets/changelog-github": "^0.6.0",
		"@changesets/cli": "^2.30.0",
		"typescript": "^5.9.3"
	},
	"engines": {
		"node": ">=20.0.0"
	},
	"packageManager": "pnpm@9.15.0"
}
